# Amadeus 全功能对齐 Akashic Lesson 推进总计划

> 这是一份“总路线图 + Lesson 复现切片”的主计划，不是假装精确到 40 节课后每个文件名的伪细案。原因很简单：`Amadeus` 已经不是空仓库，后续每个阶段都会改变代码边界。真正负责的做法是：
>
> 1. 先把完整迁移主线拆成按 Lesson 推进的 Akashic 对齐复现切片。
> 2. 只把最近 1 个阶段写成可直接执行的教学 + 实现细案。
> 3. 每进入下一个阶段前，再基于当时真实代码写该阶段的细案。
>
> 这份计划还有一个额外约束：**不走“先把 40 节课简化实现全堆完，最后统一大重构”的路线。**
>
> 采用的策略是：
>
> 1. 每节课都先做 Akashic 源码课，再做 Amadeus 复现练手。
> 2. 每节课只复现一段 Akashic 真实设计，保证你能真正讲清。
> 3. 每节课后做一次复盘、eval/smoke 沉淀和 Akashic gap audit。
> 4. 允许“分步复现”，不允许“为了快而偏离 Akashic”。

## 0. 计划前提

### 这份计划里的“对齐 Akashic”是什么意思

这里按 **真实设计复现 + 能力链对齐** 定义目标，而不是按 **目录结构复制** 定义目标。

也就是说，Amadeus 的目标不是把 `../akashic-agent` 原样搬过来，也不是先做一个简化 agent。目标是通过亲手复现 Akashic 的核心设计来掌握它，再在掌握后的基础上做二开和简历项目包装。

每节课默认分成两部分：

1. Akashic 源码课：定位源码、调用链、关键类型、状态流、测试或验证方式，讲清设计动机和复杂点。
2. Amadeus 复现练手：在确认理解后，对照 Akashic 在 Amadeus 中复现对应设计，并补验证和复盘。

必须复现和掌握的核心能力链：

1. Prompt / Context
2. Session / History
3. Provider / Passive Runtime
4. Markdown Memory
5. Vector Memory / Retrieval
6. Tool Runtime
7. Passive Agent Loop
8. Lifecycle / Plugin System
9. Scheduler / Proactive / Drift
10. Observability / Dashboard / Eval

不默认照搬的内容：

- Akashic 的历史目录结构
- 未经验证的旧模块
- 为了兼容旧行为而存在的包袱

### 当前范围假设

默认先追求 **核心能力链真实复现与完整理解**，再按需要补外层 transport。

这意味着：

- 必做：tool loop、plugin、proactive、drift、observability、eval
- 后做：多渠道适配、UI 打磨、历史插件生态扩展

如果后续你明确要做 “Akashic 级别的 Telegram / QQ / dashboard transport 全对齐”，就在第 5 阶段后加 transport 扩展支线。

### Evaluation 贯穿策略

Evaluation 不是最后阶段才补的功能，而是每个阶段都要沉淀的行为级验证资产。

每节课至少回答：

1. 本课 Akashic 对应行为是什么。
2. Amadeus 复现后应该通过哪些 focused tests。
3. 哪些 smoke/eval case 可以长期保留，防止后续 loop、tool、memory、plugin、proactive 行为漂移。

后期的 eval harness / dashboard 负责统一运行和展示这些 case；前期每节课就要开始积累 case。

---

## 1. 当前阶段地图

### 现在已经实现了什么

从当前代码和测试看，`Amadeus` 已经有一条可运行主链：

```text
CLI chat
-> build_passive_app()
-> PassiveRuntime.run_turn()
-> ContextBuilder.render()
-> LLMProvider.chat()
-> parse_response()
-> SessionManager.save()
-> TurnCommitted
-> Markdown memory maintenance
-> vector ingest / retrieval
```

对应证据：

- `amadeus/bootstrap.py`
- `amadeus/runtime.py`
- `amadeus/memory.py`
- `amadeus/vector_memory.py`
- `tests/test_bootstrap.py`
- `tests/test_runtime.py`
- `tests/test_session_memory_runtime.py`
- `tests/test_vector_memory.py`

当前测试基线：

```powershell
New-Item -ItemType Directory -Force 'C:\Users\Zinc\AppData\Local\amadeus-pytest-temp' | Out-Null
$tmp = 'C:\Users\Zinc\AppData\Local\amadeus-pytest-temp'
$env:TEMP = $tmp
$env:TMP = $tmp
uv run --extra dev pytest -q
```

当前结果：

```text
79 passed
```

### 主链路是什么

当前主链不是 agent loop，而是 **single-shot passive runtime**：

```text
user input
-> render prompt/context
-> call provider once
-> parse assistant text
-> persist user/assistant messages
-> emit TurnCommitted
-> refresh markdown memory
-> optional vector retrieval on next turn
```

### 哪些能力还是半成品

已经具备部分能力，但还需要继续做 Akashic 对齐复现：

- Vector memory 只完成了 retrieval evidence 链，尚未暴露成 tool / loop / forget runtime
- Runtime 已经有单步 tool loop，尚未完整复现 Akashic 的多步 passive reasoner loop
- Tool Runtime 已经具备 Registry / Executor / Hook Pipeline 基础，还需要继续对照 Akashic 做 loop、trace、guard 和 lifecycle 接入
- 没有 Lifecycle phases 和 Plugin System
- 没有 Scheduler / Proactive / Drift runtime
- 没有系统级 observability / dashboard / eval harness，但每课都要开始沉淀 eval/smoke case

### 下一个最值得做的 Akashic 对齐复现切片

**Lesson 1 不是直接写功能，而是先把验证基线收口。**

原因：

- 你现在的测试不是代码失败，而是默认临时目录权限失败。
- 如果不先把验证入口固定下来，后续 Lesson 会重复踩“环境误报”。
- 工程上先修验证，再堆能力，比“边开发边赌环境”稳得多。

**Lesson 2 到 Lesson 7 进入 Tool Runtime 的 Akashic 对齐复现切片。**

原因：

- Tool Runtime 是 Passive Loop、Plugin、Proactive 的共同前置。
- 当前最卡主线的不是 retrieval，而是“没有统一工具抽象”。
- 直接跳去做 agent loop，会把工具定义、执行、安全、trace 混成一层。

### 这份计划允许什么样的“分步复现”

可以接受：

- 一节课只复现 Akashic 真实设计中的一段
- 非核心 UX、配置便利性、可视化入口先轻量
- 某些阶段先复现 Akashic 机制中的基础路径，再在同一机制下继续补齐

不可以接受：

- 用临时补丁代替长期边界
- 把 fake/stub/helper 混进生产主链
- 没有稳定 contract 就提前扩功能
- 用“先通过再说”的方式绕开测试和验证
- 明知接口命名、职责分层已经失真，还留到最后一起重构
- 因为 Akashic 复杂就改写成 Amadeus 自己的简化替代

换句话说，这份计划追求的是 **Akashic 对齐的分步复现**，不是 **能跑就行的临时简化版本**。

---

## 2. 总体迁移顺序

按依赖关系，推荐总顺序是：

```text
已完成：
1. Prompt / Context
2. Session / History
3. Provider / Passive Runtime
4. Markdown Memory
5. Vector Memory / Retrieval

接下来：
6. Verification Baseline
7. Tool Runtime
8. Passive Agent Loop
9. Memory Tools / Retrieval Deepening
10. Lifecycle / Plugin Mini-Core
11. Scheduler / Outbound Ports
12. Proactive
13. Drift
14. Observability / Dashboard
15. Eval / Hardening / Gap Close
```

为什么这样排，而不是别的顺序：

- 不先稳住验证，后面所有“通过/失败”都不可信。
- 不先做 Tool Runtime，就没有干净的 tool loop 插入点。
- 不先做 Passive Loop，Plugin 插入点会反复变化。
- 不先做 Scheduler / Outbound，Proactive 只能停留在概念上。
- 不先做 Observability / Eval，后半程会越来越难定位问题。

---

## 3. Lesson 推进总表

下面这份表是 **总表**。其中最近阶段会给到更细的源码课 + 复现练手计划；更远阶段只给到 Lesson 目标，不假装精确到未来每个文件名。

### 第 1 阶段：验证基线与 Tool Runtime

#### Lesson 1：固定验证基线

本课目标：

- 固定外部临时目录测试入口
- 固定 smoke 命令
- 把“环境失败”和“代码失败”区分清楚

做完以后你应该看到：

- 一条稳定测试命令，不再依赖系统临时目录权限
- `79 passed` 可以重复复现

为什么先做它：

- 这是后面所有阶段的前置验证地基

验证命令：

```powershell
New-Item -ItemType Directory -Force 'C:\Users\Zinc\AppData\Local\amadeus-pytest-temp' | Out-Null
$tmp = 'C:\Users\Zinc\AppData\Local\amadeus-pytest-temp'
$env:TEMP = $tmp
$env:TMP = $tmp
uv run --extra dev pytest -q
```

正常结果：

- `79 passed`

异常含义：

- 如果出现 `pytest-of-Zinc` 权限报错，说明是环境没收口，不是功能回归
- 如果出现真实断言失败，才说明代码状态和文档不一致

#### Lesson 2：Tool Runtime 基础类型

本课目标：

- 定义 `Tool`、`ToolExecutionRequest`、`ToolResult`、`ToolTrace`

做完以后你应该看到：

- 代码里第一次出现稳定的 tool runtime 公共语言

为什么今天做它：

- 先定义运行时语言，再谈注册、执行和安全

#### Lesson 3：Tool Registry

本课目标：

- 注册、查找、导出 OpenAI-compatible tool schema

做完以后你应该看到：

- 系统第一次能明确回答“当前有哪些工具”

为什么今天做它：

- “有哪些工具” 和 “怎么执行工具” 是两个职责，必须先拆开

#### Lesson 4：Tool Executor 与 Hook Pipeline

本课目标：

- `before_execute -> execute -> after_execute`
- deny / error trace 标准化

做完以后你应该看到：

- tool 执行不再是裸函数调用，而是经过统一 runtime

为什么今天做它：

- 安全策略必须有正式挂点，不能散落进每个工具实现

#### Lesson 5：只读默认工具

本课目标：

- `fetch_messages`
- `search_messages`
- `read_file`

做完以后你应该看到：

- 已有 session / filesystem 能力被封进正式 tool contract

为什么今天做它：

- 先从低风险只读工具开始，避免把“写操作安全”提早混进来

#### Lesson 6：Bootstrap 接入 Tool Runtime

本课目标：

- `build_passive_app()` 暴露 `tool_registry` / `tool_executor`

做完以后你应该看到：

- App 已经具备 tool runtime 能力，但 passive runtime 仍保持 single-shot

为什么今天做它：

- 先在 composition 层挂能力，再侵入主执行链

#### Lesson 7：Tool Runtime 回归验证 + 代码精读

本课目标：

- 跑新测试 + 旧主链回归
- 收重复逻辑、统一命名和错误形状
- 精读 `bootstrap -> runtime -> session/tool` 边界

做完以后你应该看到：

- 现有 retrieval / memory / passive reply 没被 tool runtime 污染
- Tool Runtime 的接口已经能支撑下一阶段，而不是“勉强能跑”

为什么今天做它：

- 新基础设施最容易悄悄破坏旧主链

详细微计划：

- 参见 `docs/superpowers/plans/2026-06-11-tool-runtime-v1.md`

### 第 2 阶段：Passive Agent Loop Akashic 对齐复现

#### Lesson 8：Akashic provider tool-call 形状精读与 Amadeus 响应结构复现

Part 1：Akashic 源码课

- 精读 `agent/provider.py`、`agent/core/types.py` 中 `LLMResponse` / `ToolCall` / `LLMToolCall`
- 讲清 Akashic 如何区分最终文本和 tool call 请求

Part 2：Amadeus 复现练手：

- 定义 provider 返回普通文本和 tool call 的统一结果形状

做完以后你应该看到：

- `Amadeus` 首次区分“最终回复”和“需要继续执行工具”

#### Lesson 9：Akashic tool message 回灌模式精读与 Amadeus 单步 loop 复现

Part 1：Akashic 源码课

- 精读 `agent/tool_runtime.py` 的 `append_assistant_tool_calls()` / `append_tool_result()`
- 精读 `agent/core/passive_turn.py` 中 response.tool_calls 进入工具执行分支的调用链
- 讲清为什么 tool 中间消息要回灌给 LLM，但不等同于用户真实历史

Part 2：Amadeus 复现练手：

- provider 请求工具
- executor 执行工具
- tool result 追加进下一步消息

做完以后你应该看到：

- 一次简单工具调用可以跑通闭环

#### Lesson 9.5：既有 tool loop 实现的 Akashic 对齐审计

说明：Lesson 9.5 是计划调整后的过渡审计课，算作一次必要课程活动，但不替代 Lesson 10，也不改变后续正式 Lesson 编号。它的作用是把前面按旧计划完成的实现重新纳入 Akashic 坐标系。

Part 1：Akashic 源码课

- 对照 `agent/provider.py`、`agent/tool_runtime.py`、`agent/core/passive_turn.py`、`agent/tool_hooks/*`
- 讲清前面已写 Amadeus 代码分别对应 Akashic 哪一层

Part 2：Amadeus 复现练手：

- 不新增 runtime 行为，只做对齐审计
- 标记哪些实现可保留、哪些是过渡位置、哪些必须在 Lesson 10 前理解或补齐

做完以后你应该看到：

- 一份 Lesson 10 前的 Akashic 对齐审计文档
- 明确知道前面实现不是废掉，但不能继续按简化 loop 扩张

详细审计：

- 参见 `docs/superpowers/plans/2026-06-11-lesson-09-5-akashic-alignment-audit.md`

#### Lesson 10：Akashic Passive Reasoner 多步 loop 精读与 Amadeus 复现

Part 1：Akashic 源码课

- 精读 Akashic passive reasoner 的循环结构、step 计数、tool batch、incomplete summary 和 guard 相关逻辑
- 画出 `LLM -> tool -> LLM -> tool -> ... -> final reply` 的真实调用链
- 你复述 Akashic 多步 loop 主链后，再进入实现

Part 2：Amadeus 复现练手：

- 串行多步执行
- 到上限时返回阶段性回复

做完以后你应该看到：

- runtime 不再只会单轮 reply，而是能完成“思考 -> 调工具 -> 再思考”

#### Lesson 11：Akashic 工具消息持久化边界精读与 Amadeus 复现

本课目标：

- 持久化 assistant/tool traces
- 明确哪些消息进 session，哪些只是 runtime internal

做完以后你应该看到：

- context frame 和 tool 中间态不会污染用户真实历史

#### Lesson 12：Akashic context trim 精读与 Amadeus passive loop 接入

本课目标：

- provider context-length failure 时应用 trim plan

做完以后你应该看到：

- loop 发生超长上下文时，不是直接报错退出

#### Lesson 13：Passive Loop 阶段回归验证 + Akashic gap audit + 代码精读

本课目标：

- 完整 smoke：无工具、多工具、工具失败、步数上限
- 收口 loop 内部状态流和持久化边界

做完以后你应该看到：

- Passive Agent Loop Akashic 对齐复现成为新的主链

### 第 3 阶段：Memory Tools / Retrieval Deepening

#### Lesson 14：`recall_memory` tool

本课目标：

- vector retrieval 暴露为正式 tool

做完以后你应该看到：

- 模型可以通过 tool 主动请求记忆检索，而不只依赖 pre-retrieval

#### Lesson 15：evidence / source_ref 回源链强化

本课目标：

- recall 结果附带 evidence
- `fetch_messages` 能从 evidence 回原文

做完以后你应该看到：

- 检索结果不再只是“像事实的摘要”

#### Lesson 16：`forget_memory` / undo 设计与测试边界

本课目标：

- 明确忘记记忆的删除语义、撤销语义和 source_ref 稳定性

做完以后你应该看到：

- 有一条安全的数据删除/撤销设计，而不是直接删库

注意：

- 这一天只做设计和测试先行；真正删除行为执行前要再次确认

#### Lesson 17：query rewrite / sufficiency

本课目标：

- 简化版 query rewrite
- 检索不足时的 fallback 路径

做完以后你应该看到：

- recall tool 不再只靠原始 user text 直接查

#### Lesson 18：memory tool 链路回归验证

本课目标：

- recall / fetch / search / retrieval injection 的联合验证
- 收 evidence/source_ref 命名和回源边界

做完以后你应该看到：

- passive loop 和 memory tools 已经形成一条可用链

### 第 4 阶段：Lifecycle / Plugin Mini-Core

#### Lesson 19：phase 模型与 3 个基础阶段

本课目标：

- `before_turn`
- `prompt_render`
- `after_turn`

做完以后你应该看到：

- 插件终于有正式插入点，不必改主 loop 本体

#### Lesson 20：plugin registry / loader / config

本课目标：

- 插件注册、加载、配置读取

做完以后你应该看到：

- `Amadeus` 可以加载一个最小插件模块

#### Lesson 21：slot / requires / produces

本课目标：

- phase 间共享数据的最小协议

做完以后你应该看到：

- 插件不需要 import 主链内部实现就能协作

#### Lesson 22：prompt 注入插件

本课目标：

- 插件向 system prompt / context frame 注入 section

做完以后你应该看到：

- Prompt 扩展不再依赖手改 `ContextBuilder`

#### Lesson 23：tool hook / plugin failure isolation

本课目标：

- 插件能挂 tool hook
- 插件失败不炸主回复
- 回归 phase/slot/tool hook 组合路径

做完以后你应该看到：

- 生命周期系统成为后面 proactive / dashboard / safety 的正式扩展点

### 第 5 阶段：Scheduler / Outbound Ports

#### Lesson 24：scheduler store + fire-at parser

本课目标：

- 保存计划任务
- 计算下一次触发时间

做完以后你应该看到：

- `Amadeus` 有最小定时执行能力

#### Lesson 25：outbound port 抽象

本课目标：

- 把“发送一条消息”从 CLI/runtime 中抽出来

做完以后你应该看到：

- proactive 不再只能停留在内部 draft

#### Lesson 26：CLI / local outbound adapter

本课目标：

- 用本地 adapter 跑通发送链路

做完以后你应该看到：

- scheduler 可以驱动一条真实 outbound 路径

#### Lesson 27：计划任务 smoke + 边界审计

本课目标：

- 定时任务不重复触发
- 失败后状态可恢复
- 收 scheduler state 和 outbound 接口边界

做完以后你应该看到：

- 后续 proactive 已有可用基础设施

### 第 6 阶段：Proactive Akashic 对齐复现

#### Lesson 28：proactive source contracts

本课目标：

- `alert`
- `content`
- `context`

做完以后你应该看到：

- 主动系统的输入不再混成一锅

#### Lesson 29：pregate / judge / draft pipeline

本课目标：

- 先做 dry-run，不直接发消息

做完以后你应该看到：

- proactive 可以判断“该不该发”，但先不真正发送

#### Lesson 30：quota / dedupe / ack

本课目标：

- 防骚扰边界

做完以后你应该看到：

- 主动系统不会每轮都乱推

#### Lesson 31：real outbound enablement

本课目标：

- 把 dry-run draft 接上 outbound port

做完以后你应该看到：

- 第一条真实 proactive 消息跑通

#### Lesson 32：proactive 回归验证 + 代码精读

本课目标：

- alert/content/context 三路联合验证
- 收 quota/dedupe/ack 交界处的状态流

做完以后你应该看到：

- proactive 成为独立 runtime，而不是被动回复的补丁

### 第 7 阶段：Drift Akashic 对齐复现

#### Lesson 33：drift task contract

本课目标：

- 明确 drift task 输入、输出、finish 协议

做完以后你应该看到：

- drift 不再是“随便后台跑点东西”，而是受控任务

#### Lesson 34：drift runtime + tool safety

本课目标：

- 后台任务能调用工具
- finish 前不会无限悬挂

做完以后你应该看到：

- drift 有自己独立于 passive/proactive 的运行边界

#### Lesson 35：reference drift task smoke

本课目标：

- 跑 1 个真实 drift 任务样例
- 收 drift finish 协议和后台工具安全边界

做完以后你应该看到：

- `Amadeus` 具备 Akashic 风格的空闲任务能力

### 第 8 阶段：Observability / Dashboard / Eval

#### Lesson 36：prompt / tool / retrieval trace

本课目标：

- turn 级 trace 可查询

做完以后你应该看到：

- 每次调用为什么这样路由、裁剪、检索，可以查证

#### Lesson 37：inspection API / CLI

本课目标：

- 能查某次 turn 的 prompt sections、tool trace、source_ref

做完以后你应该看到：

- 调试不再只能靠 print

#### Lesson 38：lightweight dashboard or TUI inspector

本课目标：

- 做一个最小可视化检查入口

做完以后你应该看到：

- 至少可以快速检查 runtime 状态和 trace

#### Lesson 39：memory / proactive / drift eval harness

本课目标：

- personamem 风格用例
- proactive 行为用例
- drift finish 用例

做完以后你应该看到：

- 系统开始具备“防回归”的行为级验证

#### Lesson 40：Gap Close / 对照 Akashic 审计

本课目标：

- 对照 Akashic 能力地图做最后缺口清单
- 收尾前统一审计哪些是明确不迁移、哪些是下一轮增强

做完以后你应该看到：

- 一份明确的“已对齐 / 未对齐 / 不迁移”结论

---

## 4. 每节 Lesson 的统一执行模板

无论做到哪节 Lesson，都按这个节奏推进：

1. 先讲本课 Akashic 源码课目标
2. 讲清这节课对应的 Akashic 真实工程问题
3. 在理解 Akashic 后写或补 focused test / eval case
4. 复现 Akashic 对应设计，不顺手扩 scope
5. 做一次本课范围内的小收口
6. 跑 focused test + 主链回归
7. 做一次链路代码精读
8. 总结本课你应该学会什么

这里的“小收口”默认至少包含：

- 把临时命名收成正式命名
- 删除本次闭环里不再需要的重复分支
- 检查新增接口是否已经有清晰输入/输出/错误形状
- 确认测试测的是正式链路，不是临时绕路

目标不是本课就“做漂亮”，而是避免把明知错误的结构继续往后带。

## 5. 每个阶段的统一收口规则

每个阶段最后一课，不只是“跑一下测试”，而是固定做一次阶段硬化。默认包含：

1. 回归验证
2. Akashic gap audit
3. 边界审计
4. 局部重构
5. 代码精读
6. 学习复盘
7. 简历叙事沉淀

### 1. 回归验证

- 新增 focused test 全部通过
- 旧主链 smoke 仍然通过
- 至少验证一条真实 CLI/runtime 路径，而不只看单测

### 2. Akashic gap audit

至少回答清楚：

- 本阶段精读了 Akashic 哪些源码文件和测试
- Amadeus 已经复现了 Akashic 的哪些接口、数据流、生命周期边界和验证思想
- 哪些 Akashic 能力还没复现，但属于同一机制后续必须补齐
- 有没有任何 Amadeus 自己发明的简化替代；如果有，必须说明是否需要回收

### 3. 边界审计

至少回答清楚：

- 这一阶段新增的核心 contract 是什么
- 哪个模块负责定义它，哪个模块负责消费它
- 有没有把本该在 composition/runtime 层的逻辑偷塞进工具、插件或 provider

### 4. 局部重构

只允许做服务于当前阶段的重构，不做无关大扫除。优先收：

- 重复的数据结构
- 漏斗式 if/else 分发
- 漂移的命名
- 不一致的 error/result shape

### 5. 代码精读

必须能按当前阶段的真实主链讲清：

- 输入从哪里进入
- 中间在哪些对象/函数之间流转
- 状态在哪里落盘或持久化
- 错误在哪里被拦截、转换或上抛

### 6. 学习复盘

阶段结束时至少沉淀这五样：

- 这一阶段最关键的 1-3 个文件
- 最重要的 contract/接口
- 最容易写错的边界
- 一条最有代表性的验证命令
- 如果你自己重做，第一步先看什么

### 7. 简历叙事沉淀

阶段结束时要沉淀一段可以用于简历或面试讲述的版本：

- 这个模块解决什么工程问题
- Akashic 原设计怎么做
- Amadeus 复现了哪些关键机制
- 如何用 eval/smoke 证明不是 demo
- 哪些复杂能力后续会继续按 Akashic 补齐

## 6. 哪些债可以留到后面，哪些不能

可以延后：

- 非核心工具数量扩充
- CLI 交互细节打磨
- dashboard/TUI 的体验优化
- 更远阶段的泛化抽象

不能延后：

- 主链 contract 不稳定
- 错误形状在不同模块里各说各话
- 测试绕开真实执行链
- 临时 helper 已经承担正式职责
- 明知模块分层放错还继续叠功能

判断标准很简单：

- 如果它只影响“体验是否更完整”，可以后放。
- 如果它影响“后续阶段是不是要推翻本课边界”，就必须本课或当阶段收掉。

---

## 7. 每节 Lesson 的统一验证模板

### 通用测试命令

```powershell
New-Item -ItemType Directory -Force 'C:\Users\Zinc\AppData\Local\amadeus-pytest-temp' | Out-Null
$tmp = 'C:\Users\Zinc\AppData\Local\amadeus-pytest-temp'
$env:TEMP = $tmp
$env:TMP = $tmp
uv run --extra dev pytest -q
```

### 通用 CLI smoke

准备好 `.env` 后，用：

```powershell
uv run python -m amadeus chat "hello" --env .env --workspace-root .tmp_workspace --show-ids
```

正常结果：

- 终端打印 assistant 回复
- 同时打印 `session`、`user_message_id`、`assistant_message_id`
- `.tmp_workspace` 下出现 `sessions.db` 和 `memory/` 目录

异常含义：

- provider 配置报错：说明 `.env` 不完整
- 只有回复、没有持久化：说明 runtime 到 session/event 的链路断了
- memory 文件不刷新：说明 `TurnCommitted -> memory maintenance` 断了

---

## 8. 当前最近闭环的执行建议

如果按“现在继续推进”的角度，推荐顺序不是直接冲 Lesson 40，而是：

1. Lesson 1：固定验证基线
2. Lesson 2-7：完成 Tool Runtime Akashic 对齐复现
3. Lesson 8-13：完成 Passive Agent Loop Akashic 对齐复现

为什么不是先做 Plugin / Proactive：

- 现在最缺的是统一工具执行模型
- 没有它，后面的 loop / plugin / drift 都会耦在一起
- 先补 tool runtime，后续 Lesson 会更稳

---

## 9. 你现在最该记住的三件事

1. `Amadeus` 已经不是“从零开始”，而是有一条能跑通的 passive + memory 主链。
2. 真正的大缺口不是检索，而是 **工具执行抽象 + 多步 loop + 生命周期扩展点**。
3. 这份计划不是“先堆简化实现，最后统一重构”，而是 **每课 Akashic 源码课 + Amadeus 复现练手 + 每阶段硬化**。

